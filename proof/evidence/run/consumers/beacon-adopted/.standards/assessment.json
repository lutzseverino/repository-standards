{
  "kind": "agent-assessment",
  "claims": {
    "local-link-contract": "Read the effective link-contract guidance, app.py, docs/link-policy.json, and tests/fixtures/link-contract.md. Ran python3 app.py tests/fixtures/link-contract.md: exit 0, stdout [{\"text\": \"Manual\", \"url\": \"../../README.md\"}, {\"text\": \"Service status\", \"url\": \"https://status.example.invalid\"}], matching expected_links exactly. The policy contains scope local-only, network false, and the correct reference_case path. Source reads a local UTF-8 file and extracts with re.findall; it contains no HTTP operations. No fixture URL was fetched. README describes this relationship without suggesting target validation.",
    "readme": "Read the original README, CONTRIBUTING.md, app.py, installed guidance, and the generated fixture (there were no existing test programs). Ran app.py --help (exit 0), the fixture (exit 0 with the JSON reproduced in README), and tests/fixtures/absent.md (exit 2, empty stdout, usage and missing-file error on stderr). Temporary probes inside .standards confirmed no-link text yields [] with exit 0, and an anchor plus nested-label, destination-parentheses, and defined reference-style examples yields only the anchor object with exit 0; temporary probe was removed. README now follows the required title/purpose, Command reference table, Worked inventory, Boundaries, Operator notes order, documents Python 3 and output redirection grounded in stdout behavior, and links the actual policy fields. Preserved Handbook inventory identity, editorial pre-archive use, external handbook source location, URLs as data, and the unchanged employer-owned editorial approval policy. No parser changes or ordinary triage workflow were required."
  }
}
