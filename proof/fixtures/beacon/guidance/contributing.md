# Contribution governance

Write a short CONTRIBUTING.md for this consumer. Require a reproducing Markdown
input and observed JSON when reporting extraction errors. For parser changes,
require a regression case and a statement of whether nested Markdown support
changes. Discover and document the repository's real validation command. This
default governance declaration is absent from the compact profile.
