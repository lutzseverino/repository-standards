# Full operations record

The full profile owns .beacon/operations.toml as one complete file. Its capture
section supplies a repeatable operator convention: UTF-8 Markdown input and
JSON output stored outside the input document. Its command check verifies that
the consumer exposes a working help interface. The compact replacement omits
this guidance and that check; it owns only the compact exact file.
